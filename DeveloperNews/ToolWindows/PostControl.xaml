<UserControl x:Class="DeveloperNews.ToolWindows.PostControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vsshell="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.15.0"
             xmlns:local="clr-namespace:DeveloperNews.ToolWindows"
             mc:Ignorable="d" 
             d:DesignHeight="120" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Label Name="Title" 
               Padding="5 10 5 0"
               Grid.Row="0"
               Foreground="{DynamicResource {x:Static vsshell:VsBrushes.ControlLinkTextKey}}">
            <Hyperlink TextDecorations="none" FontWeight="SemiBold">
                <TextBlock Name="lblTitle"/>
            </Hyperlink>
        </Label>

        <Label Grid.Row="1"
               Padding="5 0 5 5">
            <TextBlock Name="lblSummary" TextWrapping="WrapWithOverflow" />
        </Label>
    </Grid>
</UserControl>
